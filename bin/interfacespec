#!/usr/bin/env bash
# bin/interfacespec – thin entry-point wrapper for the InterfaceSpec pipeline
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec python3 "${REPO_ROOT}/tools/interfacespec/run_cluster_pipeline.py" "$@"
