// Stub placeholders so build succeeds; full implementations land in their
// respective milestones (frontend-views).

export function TimelineView() {
  return <div className="p-6 text-slate-500">Timeline (Gantt) view — coming soon.</div>;
}
