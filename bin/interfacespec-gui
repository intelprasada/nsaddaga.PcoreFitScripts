#!/usr/bin/env bash
# bin/interfacespec-gui – thin entry-point wrapper for the InterfaceSpec GUI
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec python3 "${REPO_ROOT}/tools/interfacespec/qtgui/main.py" "$@"
