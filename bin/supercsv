#!/usr/bin/env bash
# bin/supercsv – thin entry-point wrapper for supercsv
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec python "${REPO_ROOT}/tools/supercsv/supercsv.py" "$@"
